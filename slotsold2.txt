<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Royal Gold Slots - Vegas Edition</title>
    <style>
        /* --- CORE VARIABLES & THEME --- */
        :root {
            --gold-light: #fffacd;
            --gold-base: #ffd700;
            --gold-dark: #b8860b;
            --gold-shadow: #daa520;
            --velvet: #1a0b2e;
            --neon-blue: #00e5ff;
            --neon-purple: #d500f9;
            --neon-pink: #ff4081;
            --reel-bg: #090a0f;
            --high-roller: #ff4500; /* Orange glow for high bets */
        }

        * { box-sizing: border-box; user-select: none; }

        body {
            margin: 0;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background: radial-gradient(circle at center, #2b1d42 0%, #000000 100%);
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            overflow: hidden;
            transition: box-shadow 0.5s ease;
        }

        /* --- HIGH ROLLER AFFECT --- */
        body.high-roller {
            box-shadow: inset 0 0 100px var(--high-roller);
        }

        /* --- MACHINE CONTAINER --- */
        .slot-machine {
            background: linear-gradient(135deg, #1f1235 0%, #0f0518 100%);
            border: 4px solid var(--gold-dark);
            border-radius: 24px;
            box-shadow: 
                0 0 30px rgba(212, 175, 55, 0.3),
                inset 0 0 20px rgba(0, 0, 0, 0.8);
            padding: 20px;
            width: 100%;
            max-width: 500px;
            position: relative;
            transition: border-color 0.5s ease;
        }

        .slot-machine.high-roller-mode {
            border-color: var(--high-roller);
            box-shadow: 0 0 50px rgba(255, 69, 0, 0.4);
        }

        /* --- HEADER & VIP INDICATOR --- */
        .machine-header {
            text-align: center;
            margin-bottom: 15px;
        }

        .vip-badge {
            display: inline-block;
            font-size: 0.8rem;
            color: #aaa;
            background: rgba(0,0,0,0.5);
            padding: 2px 8px;
            border-radius: 4px;
            margin-bottom: 5px;
            opacity: 0;
            transition: opacity 0.5s;
        }
        .vip-badge.active { opacity: 1; color: var(--high-roller); font-weight: bold; text-shadow: 0 0 5px var(--high-roller); }

        h1 {
            margin: 0;
            font-size: 2.5rem;
            background: linear-gradient(to bottom, var(--gold-light), var(--gold-base));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 2px 4px rgba(0,0,0,0.5);
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        .jackpot-counter {
            background: #000;
            border: 2px solid var(--gold-shadow);
            color: var(--neon-pink);
            font-family: 'Courier New', monospace;
            font-size: 1.2rem;
            padding: 5px 15px;
            border-radius: 4px;
            margin-top: 5px;
            box-shadow: 0 0 10px var(--neon-pink);
            display: inline-block;
        }

        /* --- REELS AREA --- */
        .reels-container {
            position: relative;
            background: var(--reel-bg);
            border: 4px solid #555;
            border-radius: 12px;
            padding: 10px;
            box-shadow: inset 0 0 20px #000;
            margin-bottom: 20px;
            overflow: hidden;
        }

        .paylines-overlay {
            position: absolute;
            top: 10px;
            left: 10px;
            width: calc(100% - 20px);
            height: calc(100% - 20px);
            pointer-events: none;
            z-index: 10;
        }

        .payline-svg line {
            stroke-width: 4;
            stroke-linecap: round;
            stroke: transparent;
            transition: stroke 0.3s ease, stroke-width 0.3s ease;
        }

        .payline-active line {
            stroke: var(--neon-blue);
            stroke-width: 6;
            filter: drop-shadow(0 0 5px var(--neon-blue));
        }

        .reels-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            grid-template-rows: repeat(3, 1fr);
            gap: 4px;
            height: 240px;
        }

        .reel-cell {
            background: white;
            border-radius: 4px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3.5rem;
            position: relative;
            box-shadow: inset 0 0 10px rgba(0,0,0,0.2);
            transition: transform 0.1s;
        }

        .reel-cell.win {
            background: #e0ffe0;
            animation: pulse-win 0.5s infinite alternate;
            border: 2px solid #4caf50;
        }

        @keyframes pulse-win {
            from { transform: scale(1); filter: brightness(1); }
            to { transform: scale(1.1); filter: brightness(1.3); }
        }

        .spinning .reel-cell {
            filter: blur(8px);
            opacity: 0.8;
        }

        /* --- CONTROLS & UI --- */
        .info-panel {
            display: flex;
            justify-content: space-between;
            background: rgba(0,0,0,0.3);
            padding: 10px 20px;
            border-radius: 8px;
            margin-bottom: 15px;
            border: 1px solid var(--gold-dark);
        }

        .stat-box {
            text-align: center;
        }

        .stat-label {
            font-size: 0.8rem;
            color: #aaa;
            text-transform: uppercase;
        }

        .stat-value {
            font-size: 1.1rem;
            font-weight: bold;
            color: var(--gold-light);
        }

        .message-display {
            height: 30px;
            text-align: center;
            color: white;
            font-weight: bold;
            font-size: 1.1rem;
            margin-bottom: 10px;
            text-shadow: 0 2px 2px black;
        }
        
        .msg-win { color: var(--neon-blue); }
        .msg-bonus { color: var(--neon-purple); }
        .msg-lose { color: #888; }

        .controls-area {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .bet-controls {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 10px;
            margin-bottom: 5px;
        }

        .btn {
            border: none;
            border-radius: 6px;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.1s, opacity 0.2s;
        }
        
        .btn:active { transform: scale(0.95); }
        .btn:disabled { opacity: 0.5; cursor: not-allowed; }

        .btn-num {
            background: var(--gold-dark);
            color: white;
            padding: 8px 16px;
            font-size: 1rem;
        }

        .btn-spin {
            background: linear-gradient(to bottom, #ff4081, #c51162);
            color: white;
            padding: 15px 40px;
            font-size: 1.5rem;
            text-transform: uppercase;
            box-shadow: 0 4px 15px rgba(255, 64, 129, 0.4);
            border: 2px solid #ff80ab;
        }

        .btn-spin:hover:not(:disabled) {
            background: linear-gradient(to bottom, #ff6095, #e00074);
            box-shadow: 0 6px 20px rgba(255, 64, 129, 0.6);
        }

        .bet-display {
            font-size: 1.2rem;
            color: white;
            text-align: center;
        }

        .bet-display span {
            color: var(--gold-base);
        }

        /* --- PAYTABLE TOGGLE --- */
        .paytable-btn {
            position: absolute;
            top: 20px;
            right: 20px;
            background: transparent;
            border: 1px solid var(--gold-dark);
            color: var(--gold-dark);
            padding: 5px 10px;
            font-size: 0.8rem;
            border-radius: 4px;
            cursor: pointer;
            z-index: 20;
        }
        .paytable-btn:hover { background: rgba(255,215,0,0.1); color: white; }

        .paytable-modal {
            display: none;
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.8);
            z-index: 100;
            justify-content: center;
            align-items: center;
        }
        .paytable-content {
            background: #1a1d29;
            border: 2px solid var(--gold-base);
            padding: 20px;
            border-radius: 12px;
            max-width: 90%;
        }
        .close-modal {
            float: right;
            background: none;
            border: none;
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
        }

        @media (max-width: 480px) {
            .reels-grid { height: 200px; }
            .reel-cell { font-size: 2.5rem; }
            h1 { font-size: 2rem; }
            .btn-num { padding: 6px 12px; font-size: 0.9rem; }
        }
    </style>
</head>
<body>

    <div class="slot-machine" id="machine">
        <button class="paytable-btn" onclick="togglePaytable()">📜 PAYTABLE</button>
        
        <div class="machine-header">
            <div class="vip-badge" id="vip-badge">HIGH ROLLER MODE ACTIVE</div>
            <h1>Royal Gold</h1>
            <div class="jackpot-counter">JACKPOT: $<span id="jackpot">12,450</span></div>
        </div>

        <div class="reels-container">
            <svg class="paylines-overlay" viewBox="0 0 300 240" preserveAspectRatio="none">
                <line x1="0" y1="0" x2="300" y2="0" id="line-0" class="payline-svg-line"/>
                <line x1="0" y1="80" x2="300" y2="80" id="line-1" class="payline-svg-line"/>
                <line x1="0" y1="160" x2="300" y2="160" id="line-2" class="payline-svg-line"/>
                <line x1="0" y1="0" x2="300" y2="240" id="line-3" class="payline-svg-line"/>
                <line x1="300" y1="0" x2="0" y2="240" id="line-4" class="payline-svg-line"/>
            </svg>

            <div class="reels-grid" id="grid"></div>
        </div>

        <div class="info-panel">
            <div class="stat-box">
                <div class="stat-label">Balance</div>
                <div class="stat-value">$<span id="balance">1000.00</span></div>
            </div>
            <div class="stat-box">
                <div class="stat-label">Win</div>
                <div class="stat-value" id="last-win">$0.00</div>
            </div>
        </div>

        <div class="message-display" id="message">Spin to win BIG!</div>

        <div class="controls-area">
            <div class="bet-controls">
                <button class="btn btn-num" onclick="adjustBet(-1)">-1</button>
                <button class="btn btn-num" onclick="adjustBet(-10)">-10</button>
                <div class="bet-display">
                    Bet: <span id="bet-amount">10</span>
                </div>
                <button class="btn btn-num" onclick="adjustBet(10)">+10</button>
                <button class="btn btn-num" onclick="adjustBet(1)">+1</button>
                <button class="btn btn-num" onclick="setBetMax()" style="color:var(--neon-pink)">MAX</button>
            </div>
            <button class="btn btn-spin" id="spin-btn" onclick="spin()">SPIN</button>
        </div>
    </div>

    <div class="paytable-modal" id="paytable">
        <div class="paytable-content">
            <button class="close-modal" onclick="togglePaytable()">&times;</button>
            <h2 style="color: var(--gold-base);">Winning Combinations</h2>
            <ul style="line-height: 1.8; padding-left: 20px; color: #ddd;">
                <li><strong>3 Wilds (👑)</strong> - Jackpot x100 (High Stakes Bonus)</li>
                <li><strong>3 Diamonds (💎)</strong> - Royal x25</li>
                <li><strong>3 Bells (🔔)</strong> - Gold x15</li>
                <li><strong>3 Grapes (🍇)</strong> - Silver x10</li>
                <li><strong>3 Scatters (🎲)</strong> - 15 Free Spins</li>
                <li><em><strong>Tip:</strong> Bets over $500 trigger a 1.5x Multiplier!</em></li>
            </ul>
        </div>
    </div>

    <script>
        // --- GAME CONFIGURATION ---
        const SYMBOLS = ['🍒', '🍋', '🍇', '🍉', '🔔', '💎', '7️⃣', '👑', '🎲'];
        
        // UPDATED: Higher payouts to make winning feel better!
        const PAYOUTS = {
            '🍒': 4, '🍋': 5, '🍇': 7, '🍉': 9, '🔔': 12, '💎': 25, '7️⃣': 40, '👑': 100, '🎲': 0
        };

        // UPDATED: Slightly looser weights (More frequent mid-tier wins)
        const WEIGHTS = [
            {sym: '🍒', w: 30}, {sym: '🍋', w: 25}, {sym: '🍇', w: 20}, {sym: '🍉', w: 15},
            {sym: '🔔', w: 10}, {sym: '💎', w: 5}, {sym: '7️⃣', w: 3}, {sym: '👑', w: 1}, {sym: '🎲', w: 4}
        ];

        let balance = parseFloat(localStorage.getItem('royal_balance')) || 1000.00;
        let bet = 10;
        let jackpot = 12450.00;
        let isSpinning = false;
        let freeSpins = 0;

        const balanceEl = document.getElementById('balance');
        const betEl = document.getElementById('bet-amount');
        const winEl = document.getElementById('last-win');
        const msgEl = document.getElementById('message');
        const gridEl = document.getElementById('grid');
        const spinBtn = document.getElementById('spin-btn');
        const jackpotEl = document.getElementById('jackpot');
        const machineEl = document.getElementById('machine');
        const vipBadge = document.getElementById('vip-badge');

        function initGrid() {
            gridEl.innerHTML = '';
            for(let r=0; r<3; r++) {
                for(let c=0; c<3; c++) {
                    let cell = document.createElement('div');
                    cell.className = 'reel-cell';
                    cell.id = `cell-${r}-${c}`;
                    gridEl.appendChild(cell);
                }
            }
        }

        function updateUI() {
            balanceEl.innerText = balance.toFixed(2);
            betEl.innerText = bet;
            winEl.innerText = '$0.00';
            jackpotEl.innerText = Math.floor(jackpot).toLocaleString();
            localStorage.setItem('royal_balance', balance.toString());

            // VIP/High Roller Check
            if (bet >= 500) {
                document.body.classList.add('high-roller');
                machineEl.classList.add('high-roller-mode');
                vipBadge.classList.add('active');
            } else {
                document.body.classList.remove('high-roller');
                machineEl.classList.remove('high-roller-mode');
                vipBadge.classList.remove('active');
            }
        }

        function adjustBet(amount) {
            if(isSpinning) return;
            let newBet = bet + amount;
            if(newBet < 1) newBet = 1;
            if(newBet > balance) newBet = balance;
            bet = newBet;
            updateUI();
        }

        function setBetMax() {
            if(isSpinning) return;
            bet = balance;
            if(bet < 1) bet = 1;
            updateUI();
        }

        function getRandomSymbol() {
            let totalWeight = WEIGHTS.reduce((sum, item) => sum + item.w, 0);
            let random = Math.random() * totalWeight;
            for (let item of WEIGHTS) {
                if (random < item.w) return item.sym;
                random -= item.w;
            }
            return '🍒';
        }

        function spin() {
            if (isSpinning) return;
            if (balance < bet) {
                msgEl.innerText = "Insufficient Funds! 📉";
                msgEl.className = "msg-lose";
                return;
            }

            balance -= bet;
            jackpot += (bet * 0.1); 
            updateUI();
            
            msgEl.innerText = "Spinning...";
            msgEl.className = "";
            document.querySelectorAll('.reel-cell').forEach(c => c.classList.remove('win'));
            document.querySelector('.paylines-overlay').classList.remove('payline-active');
            
            isSpinning = true;
            spinBtn.disabled = true;

            let results = [];
            for(let i=0; i<9; i++) {
                results.push(getRandomSymbol());
            }

            // Animation
            const cells = document.querySelectorAll('.reel-cell');
            let speed = 100; 
            
            const interval = setInterval(() => {
                cells.forEach(cell => cell.innerText = getRandomSymbol());
                if(speed > 20) speed -= 5;
            }, speed);

            setTimeout(() => {
                clearInterval(interval);
                isSpinning = false;
                spinBtn.disabled = false;
                
                for(let i=0; i<9; i++) {
                    cells[i].innerText = results[i];
                }

                checkWin(results);
            }, 2000);
        }

        function checkWin(results) {
            // Calculate High Roller Multiplier (1.5x if bet >= 500)
            let highRollerBonus = (bet >= 500) ? 1.5 : 1.0;
            
            let winAmount = 0;
            let winningLines = [];

            const checkLine = (idxs, symbol) => {
                let isWin = true;
                let wilds = 0;
                for (let idx of idxs) {
                    let sym = results[idx];
                    if (sym === '🎲') { isWin = false; break; }
                    if (sym === '👑') wilds++;
                    else if (sym !== symbol) { isWin = false; break; }
                }
                return isWin ? { amount: (bet * PAYOUTS[symbol]) * highRollerBonus, line: idxs } : null;
            };

            const lines = [
                { idxs: [0,1,2], name: 'top' },
                { idxs: [3,4,5], name: 'mid' },
                { idxs: [6,7,8], name: 'bot' },
                { idxs: [0,4,8], name: 'diag1' },
                { idxs: [2,4,6], name: 'diag2' }
            ];

            for (let line of lines) {
                let sym = results[line.idxs[0]];
                if (sym === '🎲') continue; 
                
                let res = checkLine(line.idxs, sym);
                if (res) {
                    winAmount += res.amount;
                    winningLines.push(line.name);
                }
            }

            let scatterCount = results.filter(s => s === '🎲').length;
            
            if (scatterCount >= 3) {
                let bonusWin = (scatterCount * 5) * bet * highRollerBonus;
                winAmount += bonusWin;
                msgEl.innerHTML = `BONUS! 🎲 <span style="color:var(--neon-purple)">${freeSpins} Free Spins</span>`;
                freeSpins += 3;
                msgEl.className = "msg-bonus";
            } else if (winAmount > 0) {
                winningLines.forEach(lName => {
                    let ids = lines.find(l => l.name === lName).idxs;
                    document.getElementById(`line-${lName}`).style.stroke = "var(--neon-blue)";
                    ids.forEach(id => document.getElementById(`cell-${id}`).classList.add('win'));
                });
                
                balance += winAmount;
                msgEl.innerHTML = `WIN! +$${winAmount.toFixed(2)} 💰 (x${highRollerBonus} Bonus)`;
                msgEl.className = "msg-win";
            } else {
                msgEl.innerText = "Try again!";
                msgEl.className = "msg-lose";
            }
            
            updateUI();
        }

        function togglePaytable() {
            let p = document.getElementById('paytable');
            p.style.display = p.style.display === 'flex' ? 'none' : 'flex';
        }

        initGrid();
        updateUI();

    </script>
</body>
</html>
